@using HSP.Service.Dtos.EmailDto;
@model ResetPasswordDto
@{
  Layout = "/Views/Emails/_EmailLayout.cshtml";
}
<p>Chào @Model.FullName,</p>
<p>Cảm ơn bạn đã đăng ký. Vui lòng nhấn vào link dưới đây để xác nhận email:</p>
<p>
  <a href="@Model.ResetUrl" class="btn">Xác nhận Email</a>
</p>
